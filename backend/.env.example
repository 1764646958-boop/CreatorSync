# CreatorSync backend service port
PORT=3001

# Comma-separated allowed origins. Use * for local development or open API access.
CORS_ORIGIN=*

# Local JSON file for publish history records
PUBLISH_HISTORY_FILE=./data/publish-history.json
