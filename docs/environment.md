# 环境变量

请复制根目录 `.env.example` 为 `.env` 后按需调整。

| 变量名 | 默认值 | 说明 |
| --- | --- | --- |
| `BACKEND_PORT` | `4000` | Express 服务监听端口 |
| `NEXT_PUBLIC_API_BASE_URL` | `http://localhost:4000` | 前端访问后端 API 的基础地址 |
