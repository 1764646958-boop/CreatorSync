# 开发规范

- 使用 npm workspaces 管理 frontend 与 backend。
- 前端使用 Next.js、TypeScript 与 Tailwind CSS。
- 后端使用 Express 与 TypeScript。
- 新增环境变量时必须同步更新根目录 `.env.example`。
- 新增文档时放置在 `docs/` 目录下。
- 每个 PR 应聚焦单一目标，避免夹带无关业务功能。
